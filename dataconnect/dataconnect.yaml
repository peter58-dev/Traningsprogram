specVersion: "v1beta"
serviceId: "workouts"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "workouts-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
